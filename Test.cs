using System;
using PolyEngine;

namespace TestXNA4
{
	public class Test : MonoBehaviour
	{
		float rot = 0;

		public override void Update()
		{
			//Vector3 move = new Vector3(Input.GetAxis("Horizontal"), Input.GetAxis("Vertical")*-1, 0);
			//rot += Input.GetAxis("Right Horizontal") * Time.deltaTime * 50;
			//transform.Translate(move * Time.deltaTime * 10);
			//transform.Rotate(new Vector3(0, 0, rot));
		}

	}

}

